# گروه صنعتی البرز — وب‌سایت

سایت استاتیک چندصفحه‌ای (HTML/CSS/JS ساده، بدون نیاز به build) برای گروه صنعتی البرز
و سه شاخه‌ی آن: چوب، فلز و سرمایه‌گذاری.

## ساختار پروژه

```
index.html               صفحه‌ی اصلی گروه، فارسی (معرفی سه شاخه + تاریخچه + تماس)
woodwork/index.html      البرز چوب (کابینت البرز)، فارسی
metalwork/index.html     البرز فلز (درب و پنجره)، فارسی
investment/index.html    البرز سرمایه‌گذاری، فارسی
en/index.html            همان صفحه‌ی اصلی، نسخه‌ی انگلیسی
en/woodwork/index.html   البرز چوب، نسخه‌ی انگلیسی
en/metalwork/index.html  البرز فلز، نسخه‌ی انگلیسی
en/investment/index.html البرز سرمایه‌گذاری، نسخه‌ی انگلیسی
css/style.css            استایل مشترک همه‌ی صفحات (فارسی و انگلیسی)
js/script.js             منطق مشترک: منو، اسکرول نرم، گالری، لایت‌باکس
images/                  تمام عکس‌ها، لوگو و بنرهای Open Graph
```

هر سه شاخه از همان هدر، فوتر، فونت و پالت رنگی گروه استفاده می‌کنند — فقط محتوا و
آیکون‌ها فرق دارد. جابه‌جایی بین شاخه‌ها از طریق دکمه‌های بالای صفحه ("چوب"، "فلز"،
"سرمایه‌گذاری"، "خانه گروه") انجام می‌شود.

## اطلاعات تماس

فایل `js/script.js`، بالای فایل:

```js
const SITE = {
  phone: '0917 768 4838',
  phoneHref: 'tel:+989177684838',
  instagram: 'alborz.titanium1',
  whatsapp: null
};
```

این مقدار در همه‌ی صفحات (گروه، چوب، فلز، سرمایه‌گذاری) یکسان استفاده می‌شود، چون
شماره‌ی جداگانه‌ای برای هر شاخه نداریم. دفتر استانبول (برای همه‌ی صفحات) مستقیم در
HTML هر صفحه نوشته شده: `+90 534 767 688`.

## نکاتی که قبل از انتشار باید بررسی شوند

- [ ] **شماره‌ی استانبول را دوباره چک کنید.** `+90 534767688` فقط ۹ رقم بعد از کد
  کشور دارد؛ موبایل‌های ترکیه معمولاً ۱۰ رقم‌اند (5XX XXX XX XX). ممکن است یک رقم جا
  افتاده باشد.
- [ ] **آدرس اینستاگرام را چک کنید.** `@alborz.titanium1` الان روی صفحه‌ی چوب (کابینت
  البرز) گذاشته شده، چون همون‌جا داده شده بود — ولی اسم «تیتانیوم» بیشتر به فلز می‌خوره.
  اگر واقعاً برای فلز است، بگویید تا جابه‌جا کنم.
- [ ] **آدرس دقیق‌تر دفتر استانبول.** الان فقط «N Logo، استانبول» نوشته شده — اگر
  پلاک/خیابان/منطقه‌ی دقیق‌تری دارید، برای نقشه و اعتبار بهتر است اضافه شود.
- [ ] **عکس برای البرز فلز و البرز سرمایه‌گذاری.** این دو صفحه فعلاً گالری ندارند
  (بخش «پروژه‌های شاخص» در فلز، فقط متنی است). هر وقت عکس آماده شد، خبر بدهید تا
  گالری مشابه صفحه‌ی چوب اضافه کنیم.
- [ ] شماره تلفن یا اینستاگرام جداگانه برای البرز فلز و البرز سرمایه‌گذاری (اختیاری —
  فعلاً از شماره‌ی مشترک گروه استفاده می‌کنند)
- [ ] ساعات کاری
- [ ] گواهی‌نامه‌ها یا نظرات مشتریان (در صورت وجود)
- [ ] دامنه‌ی نهایی سایت

## آیکون سایت (favicon) و تصویر پیش‌نمایش لینک (Open Graph)

گوگل برای نمایش آیکون واقعی سایت (به‌جای آیکون خاکستری پیش‌فرض) در نتایج جست‌وجو
حداقل به یک favicon مربعی ۴۸×۴۸ پیکسل نیاز دارد؛ نسخه‌ی قبلی فقط ۳۲×۳۲ بود. الان
هر صفحه چند سایز (۳۲، ۱۹۲، ۵۱۲) به‌علاوه یک `favicon.ico` در ریشه‌ی سایت دارد.

برای تصویر پیش‌نمایش هنگام اشتراک‌گذاری لینک (واتساپ، تلگرام، توییتر و بنر گوگل)،
به‌جای عکس‌های عمودی پروژه (که در قاب افقی بد کراپ می‌شدند) دو تصویر برند‌شده‌ی
۱۲۰۰×۶۳۰ ساخته شده: `images/og-banner-group.jpg` (خانه/فلز/سرمایه‌گذاری) و
`images/og-banner-woodwork.jpg` (چوب). اگر بعداً عکس یا رنگ لوگو عوض شد، این دو
فایل هم باید دوباره ساخته شوند تا با نسخه‌ی جدید هماهنگ بمانند.

اگر لوگو یا تصاویر هیرو را عوض کردید، حتماً `favicon-*.png`، `favicon.ico` و
`og-banner-*.jpg` را هم به‌روزرسانی کنید — این‌ها به‌صورت خودکار از فایل اصلی
ساخته نمی‌شوند.

## نسخه‌ی انگلیسی سایت

هر ۴ صفحه‌ی فارسی، معادل انگلیسی‌شان را زیر پوشه‌ی `en/` دارند (مثلاً `woodwork/`
↔ `en/woodwork/`). یک دکمه‌ی کوچک "EN" / "فارسی" در نویگیشن هر صفحه (هم دسکتاپ، هم
منوی موبایل) بین دو زبان جابه‌جا می‌کند. هر صفحه با `<link rel="alternate"
hreflang="...">` به معادل زبان دیگرش وصل شده تا گوگل بفهمد این دو نسخه‌ی یک صفحه‌اند
و نسخه‌ی درست را بر اساس زبان کاربر نشان بدهد — این تگ‌ها هم در `<head>` هر صفحه و
هم در `sitemap.xml` تکرار شده‌اند.

اگر محتوای فارسی یک صفحه را عوض کردید، حتماً معادل انگلیسی‌اش را هم به‌روزرسانی
کنید — ترجمه به‌صورت خودکار همگام نمی‌ماند.

## کلیدواژه‌های سئو (SEO keywords)

برای تقویت رتبه در جست‌وجوهای مرتبط با نام‌های مختلفی که مردم برای این مجموعه به کار
می‌برند، این کلیدواژه‌ها به‌صورت متا-تگ `keywords` و داده‌ی ساخت‌یافته (JSON-LD،
schema.org) به هر صفحه اضافه شده‌اند: «کابینت البرز»، «دکوراسیون البرز»، «آهن آلات
البرز» (نام محلی البرز فلز)، «البرز بندرعباس»، «سید کامل» (نشانی)، و نام بنیان‌گذار
به هر دو شکل «حیدر صمیمی» و «حیدر صمیمی سده». یک خط اعتبارسنجی «مدیریت: خانواده
صمیمی» هم به فوتر هر صفحه اضافه شده — چون نام‌بردن از مدیریت واقعی، اعتماد بازدیدکننده
را در کسب‌وکارهای محلی ایران بالا می‌برد.

داده‌ی ساخت‌یافته‌ی JSON-LD (در `<head>` هر صفحه، قابل مشاهده با «View Page Source»)
اطلاعاتی مثل بنیان‌گذار، نشانی، تلفن و صفحه‌ی اینستاگرام را به شکلی می‌دهد که گوگل
مستقیماً می‌فهمد — این معمولاً از نوشتن همان کلمات در متن سایت، برای سئو قوی‌تر است.

## راهنمای انتشار (Deploy)

چون همه‌ی مشتریان این سایت داخل ایران هستند، هاست را بر همین اساس انتخاب کنید:

- **Vercel را استفاده نکنید** — ترافیک ایران را رسماً مسدود می‌کند.
- **Netlify و GitHub Pages** معمولاً کار می‌کنند، اما در دوره‌های تنش منطقه‌ای ممکن
  است فیلترینگ داخلی ایران سرویس‌های خارجی را مسدود کند.
- **پیشنهاد اصلی: هاست ایرانی + دامنه‌ی .ir.** چون مخاطب صد‌درصد داخلی است، مطمئن‌ترین
  گزینه است. گزینه‌های شناخته‌شده: ParsPack / IranServer، MizbanFa، MizbanCloud —
  همه از هاست استاتیک ساده پشتیبانی می‌کنند؛ کافی‌ست کل این پوشه را با FTP یا File
  Manager کنترل‌پنل آپلود کنید (ساختار پوشه‌ها را همین‌طور نگه دارید).

ریپازیتوری گیت‌هاب (github.com/Virerra/Alborz) را برای نگهداری نسخه‌ها نگه دارید.
GitHub Pages برای پیش‌نمایش سریع مشکلی ندارد، فقط برای نسخه‌ی نهایی مشتری از هاست
ایرانی استفاده کنید.

## تست محلی

```
cd alborz-site   # یا هر اسمی که پوشه را گذاشتید
python3 -m http.server 8000
```

سپس در مرورگر: `http://localhost:8000` برای خانه‌ی گروه، `http://localhost:8000/woodwork/`
برای چوب، و به همین ترتیب برای فلز و سرمایه‌گذاری. نسخه‌ی انگلیسی هم روی همان سرور
در آدرس‌های `http://localhost:8000/en/` و `http://localhost:8000/en/woodwork/` و
مشابه در دسترس است.
